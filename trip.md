# TRIPPI 개발 기획서

**작성 목적**: Claude를 활용한 앱 개발 착수용 기획 문서
**작성일**: 2026-07-09
**버전**: v0.8 (리스크 반영, 범위 확정)

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 서비스명 | TRIPPI |
| 플랫폼 | iOS / Android (React Native 기반 크로스플랫폼) |
| 타겟 사용자 | 20~30대 |
| 한 줄 정의 | **여행 루트 관리 + 캘린더 일정 관리를 메인 축**으로, 다이어리 기록과 동반자(커플/친구) 공유를 더한 라이프로그 앱 |
| 벤치마킹 | 트리플(Triple) — 여행 루트/일정 관리 UX 참고 / 시그널링(커플을 위한 캘린더, 할일, 디데이) — 커플 캘린더·디데이 UX 참고 |

### 1.1 기획 배경
기존 여행 앱(트리플 등)과 캘린더 앱을 따로 쓰면서 느낀 불편함에서 출발한 프로젝트. 여행 일정과 일상 일정이 분리되어 있어 한눈에 파악하기 어렵고, 여행 후 기록을 남길 공간이 부족하다는 문제의식을 개선 방향으로 삼음.

> ⚠️ **[확인 필요]** 구체적으로 어떤 지점이 불편했는지(예: 캘린더-여행일정 미연동, 다이어리 기능 부재, UI 복잡도 등)를 알려주시면 이 섹션과 2번 섹션을 더 구체적으로 보강하겠습니다.

### 1.2 목표
- **[메인 축]** 여행 계획(루트)과 일상 일정(캘린더)을 하나의 화면 흐름에서 관리 — 이 앱의 핵심 가치
- **[보조 축]** 캘린더에서 하루를 클릭하면 그날의 일정 + 감성 기록(다이어리)을 함께 확인/작성, 동반자와 공유
- 2030세대가 부담 없이 매일 쓸 수 있는 가볍고 감성적인 UX

> 📌 **포지셔닝 확정**: 이 앱은 "커플/친구 앱"이 아니라 **"일정관리+여행에 강한 앱"**이 메인이고, 다이어리·동반자 공유는 이를 풍부하게 해주는 보조 기능입니다. 기능 우선순위가 애매할 때는 이 축을 기준으로 판단합니다.

---

## 2. 문제 정의 및 개선 포인트

| 기존 앱 (트리플 등) | 불편했던 점 | 우리 앱의 개선 방향 |
|---|---|---|
| 여행 루트 관리는 강점 | 여행 아닌 일상 일정과 분리됨 | 캘린더 안에 여행 일정이 함께 표시됨 |
| 여행 중 기록 기능 약함 | 사진/글로 감정을 남기기 번거로움 | 캘린더 클릭 → 바로 다이어리 작성 연결 (모달창으로 확인) |
| 경로(루트) 등록 시 지도 UX | 장소를 등록하는 동안 지도가 자꾸 임의로 이동해서 원하는 위치를 잡기 어려움 | 장소 검색/선택 후에는 지도 중심이 자동으로 움직이지 않도록 고정, 필요할 때만 사용자가 직접 이동 |
| 최초 장소 등록 | 처음 장소를 등록할 때 의도한 위치에 정확히 핀이 꽂히지 않음 | 검색 결과 선택 시 핀 위치를 명확히 미리보기로 보여주고, "이 위치로 등록" 확인 단계를 거쳐 확정 |

> 위 지도 관련 불편사항은 6번 기술스택(지도 라이브러리)과 7번 개발 우선순위(Phase 1~2)에도 반영했습니다.

---

## 3. 핵심 기능 정의

### 3.1 기능 ①: 여행 루트 관리
- 여행별로 방문 장소를 순서대로 등록/편집 (드래그 앤 드롭으로 순서 변경)
- 지도 위에 루트 시각화 (핀 + 이동 경로 표시)
- 장소별 메모, 예상 소요 시간, 방문 예정 시간대 기록
- 여행 단위(Trip)로 그룹핑 → 하나의 여행 = 여러 Day = 여러 장소
- **[개선사항]** 장소 검색/등록 중에는 지도가 임의로 움직이지 않도록 카메라 고정, 검색 결과 선택 시 핀 위치 미리보기 → "이 위치로 등록" 확인 후 확정하는 2단계 플로우 적용

### 3.2 기능 ②: 캘린더 일정 관리 (+ 여행 일정 통합 노출)
- 월간/주간 캘린더 뷰 제공
- 일반 일정(개인 스케줄)과 여행 일정을 같은 캘린더에 색상/태그로 구분해 함께 표시
- 여행 기간 동안은 해당 날짜에 "Day 1", "Day 2"처럼 여행 루트와 자동 연동된 배지 표시
- 일정 추가/수정/삭제, 알림 설정

### 3.3 기능 ③: 캘린더 클릭 → 다이어리 작성/열람
- 캘린더에서 특정 날짜 클릭 시 해당 날짜의 일정 목록 + 다이어리 작성 영역 노출
- 다이어리 꾸미기: 사진 첨부, 스티커/폰트/배경 테마 선택, 감정 태그(기분 이모지 등) — GIPHY API(스티커), Unsplash API(배경 사진), Google Fonts API(폰트) 활용
- 텍스트 기록(오늘의 한 줄 ~ 자유 기록) 작성 가능
- 여행 중인 날짜는 해당 Day의 방문 장소가 자동으로 다이어리에 태그로 붙어 "장소 기반 기록"이 가능

### 3.4 기능 ④: 동반자 연동 (커플/친구, 시그널링 벤치마킹)
- 커플뿐 아니라 **친구끼리도** 사용 가능하도록 범위 확장 (1:1 또는 다인 그룹 형태는 추후 논의)
- **초대 코드** 방식으로 연동: 한 명이 코드를 생성 → 상대방이 코드 입력 시 연동 완료
  - **코드 유효기간: 30분** (미사용 시 자동 만료, 재생성 가능)
  - **입력 시도 5회 실패 시 해당 코드 잠금** → 재생성 필요 (무작위 대입 방지)
  - 기본은 **1:1 연동**으로 설계, 다인 그룹 연동은 이후 확장 항목으로 별도 논의
- **연결 해제**: GNB(마이페이지 등)에 "연결 해제" 버튼 배치 → 클릭 시 **연결 해제 상세 페이지**로 이동, 해제 시 영향(공유 데이터가 상대방에게 더 이상 보이지 않음 등)을 안내 후 최종 확인 절차를 거쳐 해제
- 홈 화면에 동반자와의 디데이(예: 함께한 지 며칠째, 특정 기념일까지 D-day) 표시
- **공유 범위 (기본값 정책)**
  - 캘린더 일정, 여행 루트, 다이어리 모두 **기본값은 "전체 공유"**
  - 각 일정/기록 작성 시 **"비밀로 하기" 체크박스** 제공 → 체크하면 동반자에게 노출되지 않고 작성자 본인만 조회 가능
- 로그인/회원가입: **소셜 로그인** 방식 채택
  - **1순위 카카오** — 국내 2030 타겟 대상 진입장벽이 가장 낮음 (시그널링도 카카오 메인)
  - **2순위 애플** — iOS 앱스토어 심사 규정상, 제3자 소셜 로그인을 제공하면 Sign in with Apple도 필수로 함께 제공해야 함
  - **3순위 구글** — 안드로이드 사용자 및 기타 계정 커버용 (선택)

> 이 4가지 기능이 유기적으로 연결되는 것이 이 앱의 핵심 차별점입니다:
> **여행 루트 등록 → 캘린더에 자동 반영 → 날짜 클릭 시 그날의 장소와 함께 다이어리 기록 (+ 동반자와 디데이/일정 공유, 비밀 설정 시 본인만 열람)**

### 3.5 기능 ⑤: 외부 공유 (여행 일정만)
- 앱 미설치자를 포함한 **외부 사람에게는 "여행 일정(루트)"만** 공유 가능 (캘린더 개인 일정, 다이어리는 외부 공유 대상 아님)
- 공유 방식 2가지
  - **링크 복사**: 여행 상세 화면에서 공유 링크 생성 → 복사해서 카카오톡/문자 등으로 전달
  - **앱으로 이동**: 상대방이 앱을 이미 설치했다면 딥링크로 앱 내 해당 여행 상세 화면으로 바로 연결, 미설치 시 스토어 설치 페이지로 유도
- 외부 공유된 여행은 **읽기 전용**(뷰어)으로 열람만 가능, 편집 권한 없음

---

## 4. 사용자 시나리오 (예시)

1. 사용자가 '제주도 3박4일' 여행을 등록하고 Day별로 방문할 장소(루트)를 입력한다.
2. 메인화면에 있는 캘린더에 해당 여행 기간에 자동으로 "제주도 여행 Day 1~4" 배지가 표시된다.
3. 여행 중 특정 날짜(Day 2)를 클릭하면 그날 방문 예정이던 장소 목록이 보인다.
4. 사용자는 그 화면에서 바로 사진을 첨부하고 오늘 있었던 일을 다이어리로 기록한다.
5. 여행이 끝난 후에도 캘린더를 통해 그날의 기록을 다시 열람할 수 있다.

---

## 5. 정보 구조 (IA) 초안

```
홈 (여행 일정 표시(슬라이드 기능으로 보이기) + 메인 캘린더 + 동반자 디데이 표시)
├── 캘린더 뷰 (월/주)
│   └── 날짜 클릭
│       ├── 그날의 일정 리스트
│       └── 다이어리 작성/열람
├── 여행 (Trip) 탭
│   ├── 여행 목록
│   └── 여행 상세
│       ├── 루트 관리 (지도 + Day별 장소)
│       └── 여행 설정 (기간, 제목, 커버 이미지)
├── 일정 추가 (개인 일정 / 여행 연동 일정)
└── 마이페이지
    ├── 다이어리 모아보기 (타임라인)
    ├── 동반자 연결 해제 (상세 확인 페이지)
    └── 설정
```

---

## 6. 기술 스택 (제안)

| 영역 | 기술 |
|---|---|
| 프레임워크 | React Native (Expo 권장 — 배포/빌드 편의성) |
| 상태관리 | Zustand 또는 Redux Toolkit |
| 인증 | 카카오 로그인 SDK(1순위) + Sign in with Apple(iOS 필수) + Google 로그인(선택) |
| 로컬 저장 | 별도 라이브러리 없이 **Firestore 오프라인 캐싱 기능**만 활용 (자동 동기화, 구현 단순화) |
| 백엔드 | Firebase (Auth, Firestore, Storage) — 확정 |
| 지도 | **국내 우선**: 카카오맵 SDK + 카카오 로컬 API로 시작 (해외 확장은 추후 검토 항목, 6.1 참고) |
| 캘린더 UI | react-native-calendars |
| 이미지/스티커 API | GIPHY API(스티커/GIF, 무료) + Unsplash API(배경·커버 사진, 무료) + Google Fonts API(손글씨체 등 폰트, 무료) |
| 이미지 처리 | expo-image-picker, expo-image-manipulator — **업로드 전 리사이즈(가로 1080px) + 압축(품질 70~80%)로 Storage 용량 절감** |
| 알림 | expo-notifications |

> 백엔드는 **Firebase로 확정**. Firestore 실시간 리스너를 활용해 동반자 간 캘린더/다이어리 실시간 공유(비밀 항목 제외)를 구현할 예정.

### 6.1 지도 API 정책 (국내 우선 확정)
- **1단계(현재)**: 카카오맵 SDK + 카카오 로컬 API만 적용, **국내 여행만 우선 지원**
  - 개발자 계정당 첫 앱은 카드 등록 없이 넉넉한 무료 쿼터(개인 일 20만 건/법인 일 30만 건) 사용 가능
  - 국내 장소명 검색 정확도가 높아 사용자 경험에 유리
- **2단계(추후 검토)**: 해외여행 수요가 확인되면 그때 구글맵/Google Places API 추가 검토 — 처음부터 두 지도를 동시에 구현하지 않는 것으로 범위 축소
- ⚠️ **개발 방식 관련 유의사항**: 카카오맵 SDK는 순정 Expo Go 앱에서는 실행이 안 되고, `EAS Build`(커스텀 개발 빌드)가 필요해요. AI 도구로 코드를 작성(바이브코딩)하더라도 이 설정 자체는 한 번은 직접 확인이 필요한 부분이니, 프로젝트 초기에 `npx expo install`이 아니라 `EAS Build` 환경부터 세팅하고 시작하는 걸 권장해요.

---

## 7. 개발 우선순위 (MVP → 확장)

**Phase 1 (MVP)**
- 캘린더 기본 CRUD (일정 등록/조회)
- 여행(Trip) 생성 + Day별 장소 등록 (지도 없이 리스트 형태 우선, 국내 카카오 로컬 API로 검색만 우선 적용)
- 날짜 클릭 → 텍스트 다이어리 작성/조회

**Phase 2**
- 카카오맵(국내) 기반 루트 시각화 적용
- 캘린더-여행 일정 자동 연동 (배지, 색상 구분)
- 다이어리 사진 첨부 + 꾸미기(스티커/테마)
- 동반자 연동(초대 코드, 공유, 연결 해제) 구현

**Phase 3**
- 다이어리 타임라인/앨범 뷰
- 외부 공유(링크 복사/딥링크) 기능 구현
- 서비스 안정화 후 하단 배너 광고 연동 (수익화)
- 알림/위젯 등 부가 기능
- (수요 확인 시) 해외여행 지원을 위한 구글맵 연동 검토

---

## 8. 추가로 정해야 할 것 (Open Questions)

- [ ] **다인 그룹 확장 여부**: 지금은 1:1 연동으로 설계했는데, 이후 여러 명(가족 여행 그룹 등)까지 지원할지 — Phase 3 확장 항목으로 별도 논의
- [ ] **광고 SDK 선정**: Google AdMob(가장 대중적, React Native 연동 라이브러리 풍부) 등 후보 중 확정 필요
- [ ] 개인정보처리방침/이용약관 초안 작성 (트리플·시그널링 참고, 스토어 등록 전 필수)

## 9. 수익 모델

- **1단계(초기)**: 무료 서비스로 운영하며 사용자 확보에 집중, 광고 없음
- **2단계(안정화 이후)**: **하단 배너 광고**로 수익화 시작 (상단 배너는 콘텐츠 시야 방해가 커서 배제, 전면/리워드 광고는 넣지 않음 — 감성적인 다이어리 앱 특성상 몰입을 크게 깨지 않는 하단 배너가 적합)
- 추후 프리미엄(광고 제거, 추가 스티커/테마 등) 유료 모델은 확장 검토 대상으로 남겨둠

## 10. 정책 및 법적 참고사항

- **개인정보처리방침/이용약관**: 트리플, 시그널링의 정책을 참고하여 초안 작성 예정 (앱스토어/플레이스토어 등록 시 필수 항목이므로 출시 전 반드시 완비)
- **연령 제한**: 별도 최소 연령 제한 없음으로 결정
  - 참고로 국내 개인정보보호법(PIPA)상 **만 14세 미만은 법정대리인 동의**가 필요한 경우가 있어요. 이 부분은 정확히는 법률 자문이 필요한 영역이라, 개인정보처리방침 작성 시 함께 확인하는 걸 권장합니다.

## 11. 개발 방식 관련 참고

- 개발자님이 코드를 어느 정도 검토는 하지만 **대부분 AI 도구를 활용한 바이브코딩** 방식으로 진행 예정
- 이에 따라 이 기획서의 기술스택은 **문서화가 잘 되어 있고 커뮤니티가 큰 라이브러리 위주**로 우선 추천했고(Firebase, react-native-calendars 등), 카카오맵처럼 네이티브 설정이 필요한 부분은 미리 별도로 표시해뒀어요 (6.1 참고)
- 화면 정의서나 코드 작업으로 넘어갈 때는, **한 번에 여러 기능을 동시에 만들기보다 Phase 1부터 하나씩 검증하며 진행**하는 걸 추천해요 — 바이브코딩 특성상 한꺼번에 많은 기능을 요청하면 오류를 찾아내기 어려워질 수 있어요

---

*이 문서는 초안이며, 위 Open Questions에 대한 답변을 주시면 상세 기획서(화면 정의서, 기능 명세서 수준)로 발전시킬 수 있습니다.*
